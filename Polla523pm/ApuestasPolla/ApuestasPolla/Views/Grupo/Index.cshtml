@model IEnumerable<ApuestasPolla.Models.Grupo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdGrupo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdGrupo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.IdGrupo  }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdGrupo  }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdGrupo  })
        </td>
    </tr>
}

</table>
